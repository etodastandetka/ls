# Бот для сбора премиум эмодзи

Бот-утилита для сбора и сохранения премиум эмодзи из Telegram.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте бота через [@BotFather](https://t.me/BotFather)

3. Получите токен бота и вставьте его в `main.py`:
```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
```

Или создайте файл `.env`:
```
BOT_TOKEN=your_token_here
```

И обновите `main.py` для чтения из `.env`:
```python
import os
from dotenv import load_dotenv

load_dotenv()
BOT_TOKEN = os.getenv("BOT_TOKEN")
```

## Использование

1. Запустите бота:
```bash
python main.py
```

2. Отправьте боту сообщение с премиум эмодзи

3. Бот автоматически сохранит их

4. Нажмите кнопку "📋 Конфиг" для получения готового конфига

5. Скопируйте конфиг в `bot_new/config.py` в `PREMIUM_EMOJI_MAP`

## Команды

- `/start` - Начать работу с ботом
- `/config` - Получить конфиг файл
- `/list` - Показать список сохраненных эмодзи
- `/clear` - Очистить все сохраненные эмодзи

## Структура файлов

- `main.py` - Основной файл бота
- `premium_emojis.json` - Файл с сохраненными эмодзи (создается автоматически)
- `requirements.txt` - Зависимости


