# Быстрый запуск бота-модератора

## Шаг 1: Перейти в директорию бота

```powershell
cd C:\Users\User\Desktop\LUXON\bot_moderator
```

## Шаг 2: Установить токен бота

### Вариант А: Через .env файл (рекомендуется)

Создайте файл `.env` в папке `bot_moderator` со следующим содержимым:

```
MODERATOR_BOT_TOKEN=ваш_токен_бота_здесь
```

### Вариант Б: Через переменную окружения

```powershell
$env:MODERATOR_BOT_TOKEN="ваш_токен_бота_здесь"
```

## Шаг 3: Установить зависимости

```powershell
pip install -r requirements.txt
```

Или если используете виртуальное окружение:

```powershell
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

## Шаг 4: Запустить бота

```powershell
python bot.py
```

Или используйте готовый скрипт:

```powershell
.\start.bat
```

## Полная последовательность команд (копируйте по порядку):

```powershell
# 1. Перейти в директорию
cd C:\Users\User\Desktop\LUXON\bot_moderator

# 2. Установить токен (замените на свой токен)
$env:MODERATOR_BOT_TOKEN="ваш_токен_бота"

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить бота
python bot.py
```

## Проверка работы

После запуска вы должны увидеть:
```
🤖 Бот-модератор запускается...
📋 Запрещенных слов в списке: 12
```

## Остановка бота

Нажмите `Ctrl+C` в терминале для остановки бота.

